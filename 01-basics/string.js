const name = "omkar";
const count = 50;
console.log(name + count);

// string interpolation is modern way of working with String
// string function can be called in it
console.log(`my name is ${name.toUpperCase()}`);
